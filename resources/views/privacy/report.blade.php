<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,sans-serif;font-size:11px;color:#222}h1{font-size:20px}h2{font-size:15px;margin-top:20px;border-bottom:1px solid #ddd;padding-bottom:5px}.item{margin:6px 0;padding:6px;background:#f7f7f7}.meta{color:#666}</style></head><body><h1>My Digital Diary - Personal Data Report</h1><p class="meta">Generated for {{ $user->name }} ({{ $user->email }}) on {{ now()->format('Y-m-d H:i') }}</p><p><strong>Reason:</strong> {{ $requestRow->reason }}</p><p><strong>Period:</strong> {{ $requestRow->date_from?->format('Y-m-d') ?? 'All dates' }} to {{ $requestRow->date_to?->format('Y-m-d') ?? 'present' }}</p>@foreach($payload as $module=>$rows)<h2>{{ \Illuminate\Support\Str::headline($module) }}</h2>@forelse($rows as $row)<div class="item">@foreach($row->toArray() as $key=>$value)@if(!in_array($key,['user_id']))<strong>{{ \Illuminate\Support\Str::headline($key) }}:</strong> {{ is_array($value)?json_encode($value):$value }}<br>@endif @endforeach</div>@empty<p>No records for the selected period.</p>@endforelse @endforeach</body></html>